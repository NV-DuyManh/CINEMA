<Page x:Class="Cinema.QLTaiKhoan"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Cinema"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="1000"
      Title="Quản Lý Tài Khoản" Background="#FFFFFF">

    <Page.Resources>
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Height" Value="42"/>
            <Setter Property="Width" Value="135"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="10">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="10" Color="Black" Opacity="0.08" ShadowDepth="2"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                                <Setter Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="1.03" ScaleY="1.03"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.97" ScaleY="0.97"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
        </Style>

        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Height" Value="40"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="Background" Value="#F8FAFC"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1.5" Background="{TemplateBinding Background}" CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#3B82F6"/>
                                <Setter TargetName="border" Property="Background" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Margin="0,0,0,25">
            <TextBlock Text="QUẢN LÝ TÀI KHOẢN" FontSize="24" FontWeight="Black" Foreground="#0F172A"/>
            <Rectangle Height="4" Width="50" Fill="#3B82F6" HorizontalAlignment="Left" Margin="2,6,0,0" RadiusX="2" RadiusY="2"/>
        </StackPanel>

        <Border Grid.Row="1" Background="White" CornerRadius="16" Padding="25,20" Margin="0,0,0,25" BorderBrush="#F1F5F9" BorderThickness="1">
            <Border.Effect>
                <DropShadowEffect BlurRadius="30" Color="#000000" Opacity="0.05" ShadowDepth="10"/>
            </Border.Effect>

            <StackPanel>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Margin="10,5">
                        <TextBlock Text="Tài khoản" Foreground="#64748B" Margin="0,0,0,8" FontSize="11" FontWeight="Bold"/>
                        <TextBox x:Name="txtTaiKhoan" Style="{StaticResource ModernTextBox}"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Margin="10,5">
                        <TextBlock Text="Mật khẩu" Foreground="#64748B" Margin="0,0,0,8" FontSize="11" FontWeight="Bold"/>
                        <TextBox x:Name="txtMatKhau" Style="{StaticResource ModernTextBox}"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2" Margin="10,5">
                        <TextBlock Text="Họ và tên" Foreground="#64748B" Margin="0,0,0,8" FontSize="11" FontWeight="Bold"/>
                        <TextBox x:Name="txtHoTen" Style="{StaticResource ModernTextBox}"/>
                    </StackPanel>

                    <StackPanel Grid.Column="3" Margin="10,5">
                        <TextBlock Text="Chức vụ" Foreground="#64748B" Margin="0,0,0,8" FontSize="11" FontWeight="Bold"/>
                        <ComboBox x:Name="cbChucVu" Height="40" VerticalContentAlignment="Center" Background="#F8FAFC" BorderBrush="#E2E8F0">
                            <ComboBoxItem Content="Admin"/>
                            <ComboBoxItem Content="Staff"/>
                        </ComboBox>
                    </StackPanel>
                </Grid>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,25,5,0">
                    <Button x:Name="btnLamMoi" Click="btnLamMoi_Click" Content="↺ Làm mới" Background="#94A3B8" Style="{StaticResource ModernButton}"/>
                    <Button x:Name="btnXoa" Click="btnXoa_Click" Content="🗑 Xóa" Background="#F43F5E" Style="{StaticResource ModernButton}"/>
                    <Button x:Name="btnSua" Click="btnSua_Click" Content="💾 Cập nhật" Background="#F59E0B" Style="{StaticResource ModernButton}"/>
                    <Button x:Name="btnThem" Click="btnThem_Click" Content="✚ Thêm mới" Background="#10B981" Style="{StaticResource ModernButton}"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <Border Grid.Row="2" Background="White" CornerRadius="16" BorderBrush="#F1F5F9" BorderThickness="1" ClipToBounds="True">
            <DataGrid x:Name="dgNguoiDung" SelectionChanged="dgNguoiDung_SelectionChanged" 
                      AutoGenerateColumns="False" CanUserAddRows="False"
                      Background="Transparent" BorderThickness="0" GridLinesVisibility="None"
                      RowHeight="50" SelectionUnit="FullRow" IsReadOnly="True" HeadersVisibility="Column">

                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#F8FAFC"/>
                        <Setter Property="Foreground" Value="#475569"/>
                        <Setter Property="Padding" Value="15,0"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="Height" Value="45"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="#E2E8F0"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>

                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="#F8FAFC"/>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#F1F5F9"/>
                                <Setter Property="Foreground" Value="#3B82F6"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding ma_nguoi_dung}" Width="70"/>
                    <DataGridTextColumn Header="TÀI KHOẢN" Binding="{Binding tai_khoan}" Width="1.5*"/>
                    <DataGridTextColumn Header="MẬT KHẨU" Binding="{Binding mat_khau}" Width="*"/>
                    <DataGridTextColumn Header="HỌ VÀ TÊN" Binding="{Binding ho_ten}" Width="2*"/>
                    <DataGridTextColumn Header="CHỨC VỤ" Binding="{Binding chuc_vu}" Width="120"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</Page>
